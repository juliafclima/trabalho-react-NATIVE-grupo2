import { NavigationContainer } from '@react-navigation/native';
import { Tab } from './tab';

export const Rotas = () => {
  return (
    <NavigationContainer>
      <Tab />
    </NavigationContainer>
  );
};